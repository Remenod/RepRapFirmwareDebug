# Cloned Duet3D repos (each has its own git history/remote) + the Eclipse
# workspace metadata that rrf.sh keeps alongside them.
/repos/

# Downloaded toolchains
/tools/

# Stray Eclipse metadata if a build is ever run at the workspace root
/.metadata/
